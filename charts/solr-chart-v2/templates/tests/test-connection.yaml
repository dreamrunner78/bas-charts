apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "solr-chart.fullname" . }}-test-connection"
  labels:
    {{- include "solr-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "solr-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
